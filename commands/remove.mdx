---
title: "reseed remove"
description: "Remove a skill from the current project"
---

```bash
reseed remove <skill>
```

Deletes a skill directory from the project's `.agents/skills/` folder. This only removes it from the project, not from your library.

## Example

```bash
reseed remove frontend-design
```
